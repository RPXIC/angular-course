import { StringEncodePipe } from './string-encode.pipe';

describe('StringEncodePipe', () => {
  it('create an instance', () => {
    const pipe = new StringEncodePipe();
    expect(pipe).toBeTruthy();
  });
});
